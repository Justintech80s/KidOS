name: KidOS Windows Lockdown Task 1 CI

on:
  push:
    branches:
      - feat/windows-lockdown-mode
  pull_request:
    paths:
      - 'packages/contracts/**'
      - '.github/workflows/windows-lockdown-task1-ci.yml'

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @kidos/contracts test --run
      - run: pnpm --filter @kidos/contracts build
