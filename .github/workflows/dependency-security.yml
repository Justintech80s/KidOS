name: KidOS Dependency Security

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout KidOS
        uses: actions/checkout@v4

      - name: Review dependency changes when Dependency Graph is available
        continue-on-error: true
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: moderate
          fail-on-scopes: runtime

      - name: Explain optional dependency-review result
        run: echo "Dependency Review is advisory here because GitHub Dependency Graph may be disabled; Rust and Python audits below remain mandatory release gates."

  rust-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout KidOS
        uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Audit Rust dependencies
        run: cargo audit

  python-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout KidOS
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install classifier and audit tooling
        run: |
          python -m pip install --upgrade pip "setuptools>=83"
          python -m pip install -e "services/media-classifier"
          python -m pip install pip-audit

      - name: Audit Python dependencies
        run: pip-audit
